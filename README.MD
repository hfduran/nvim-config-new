## REQUIREMENTS:

### LSPs:
- install from Mason all the language servers needed
- to check the plugins loaded/enabled: `:checkhealth vim.lsp`
- to check the lsp configurations for adding new lsp, do `:help lspconfig-all`
